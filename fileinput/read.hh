#ifndef READ_HH
#define READ_HH

// Because i need to read a midi file and print out the notation
void Read(const char* filename,int file_size);

//Because i need to create a new midi file for furture use
void Create(const char* filename);

#endif // READ_HH
